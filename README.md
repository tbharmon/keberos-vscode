# keberos-vscode
 simple tool to acquire a Kerberos TGT into an isolated cache and launch Visual Studio Code (macOS) for Integrated/Windows Authentication (e.g., SQL Server, Active Directory resources).
